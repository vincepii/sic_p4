
Global setup:

  Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email
        

Next steps:

  mkdir sic_p4
  cd sic_p4
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:vincepii/sic_p4.git
  git push origin master
      

Existing Git Repo?

  cd existing_git_repo
  git remote add origin git@github.com:vincepii/sic_p4.git
  git push origin master
      

Importing a Subversion Repo?

  Click here
      

When you're done:

  Continue


